name: Deploy to GitHub Pages

on:
  push:
    branches: [master]

permissions:
  id-token: write
  contents: read
  pages: write

jobs:
  deploy:  # Changed job name from 'build' to 'deploy'
    runs-on: ubuntu-latest
    environment:  # Added environment configuration
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - name: Install marimo
        run: pip install marimo

      - name: Export notebook and copy data
        run: marimo export html-wasm state_port_country.py -o docs/ --mode run

      - name: Upload GitHub Pages artifact
        uses: actions/upload-artifact@v4
        with:
          name: github-pages
          path: docs/

      - name: Deploy to Pages
        id: deployment  # Added ID for reference
        uses: actions/deploy-pages@v4